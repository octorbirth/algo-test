#include <stdio.h>

int main(){
    printf("(A) Push Remote..");
    printf("(B) Commit (Not Push)..");
    printf("(C) Add (Not Commit)..");
    printf("(te Add (Not Commit)..");
    printf("(C) Add (Not Commit)..");
}

